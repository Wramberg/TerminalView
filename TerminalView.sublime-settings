{
  // Enable/disable colors in the terminal view. This causes a minor increase in
  // terminal response time, hence its disabled by default. Unless you are using
  // a 20 year old PC it should be fine to enable though.
  "terminal_view_show_colors": true,

  // Enable/disable debug printing to the console
  "terminal_view_print_debug": false,
}
